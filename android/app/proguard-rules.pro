# Porchlight Press (Phase 5). R8 full mode already keeps
# kotlinx.serialization, Room, Retrofit and Compose via their bundled rules.
# Keep BuildConfig fields referenced reflectively in tests.
-keep class com.charleshartman.porchlightpress.BuildConfig { *; }
