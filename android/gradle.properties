org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Release AdMob IDs are injected only via -P/-D Gradle properties from CI
# secrets (see app/build.gradle.kts). Never commit real IDs here.
