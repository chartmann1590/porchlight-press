# Phase 1 runtime dependencies (pipeline ingest + normalize + health).
# Full Phase 0 decision list also includes rapidfuzz, scikit-learn,
# llama-cpp-python for Phases 2-3; those land with their phases.
feedparser>=6.0
httpx>=0.27
defusedxml>=0.7
jsonschema>=4.20
pyyaml>=6.0
