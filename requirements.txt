# Phase 1 runtime dependencies (pipeline ingest + normalize + health).
# Phase 2 adds rapidfuzz (dedup) + scikit-learn (TF-IDF clustering).
# Phase 3 will add llama-cpp-python; it lands with that phase.
feedparser>=6.0
httpx>=0.27
defusedxml>=0.7
jsonschema>=4.20
pyyaml>=6.0
rapidfuzz>=3.0
scikit-learn>=1.3
